#include<bits/stdc++.h>
using namespace std;

int main()
{
    float a, b;
    cin>>a>>b;
    cout<<"floor "<<a<<" / "<<b<<" "<<"="<<" "<<floor(a/b)<<endl;
    // floor function floor for
    cout<<"ceil "<<a<<" / "<<b<<" "<<"="<<" "<<ceil(a/b)<<endl;
    //ceil function ceil for 
    cout<<"round "<<a<<" / "<<b<<" "<<"="<<" "<<llround(a/b)<<endl;
    //llround function for round value
    return 0;

}